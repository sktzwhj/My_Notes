之前和李根老师讨论的时候，他提到了关于数值压缩的预测问题，一直没有理解是要预测什么。　

最近在看一本压缩相关的书籍的时候，算是对这个问题有了一个较好的理解，在这里记下来。　

因为压缩实际上就关系到编码，像是huffman coding这样的方法，最基本的思想也是说给更加常见的字符更短的编码。因而编码就可以分为静态编码和动态编码。　
静态的可能来自对大量文本的一个统计，而动态，或者叫做自适应的编码方式，则可以获得更好的压缩效果。这就叫做编码的模型。　有一些模型是可以考虑文本的
上下文的，当然这也主要是针对文本压缩来讲。　举个例子，比如在q之后, u出现的概率就更高一些。在编码模型当中，一个值得关注的问题是零频问题，也有一些
已有的方法通过预设每个字符出现的最小频率等方式来避免这样的问题。

算数编码：