关于虚拟机的I/O，大致分为三类而且每一类可以看为一代。 

最早的用软件模拟硬件的时候，所有的I/O同样也是由软件模拟的，但是软件只能模拟非常简单的基本硬件，而且开销非常大。所以我更倾向于叫这种虚拟机 - 模拟器。 

然后后来就是由一个hypervisor提供一个接口，然后所有的虚拟机通过hypervisor的接口来访问I/O，因为由hypervisor这个层次，所以性能并不会很好。 

现在许多硬件的虚拟技术支持使得hypervisor可以直接将PCI-E上的设备分配给虚拟机直接访问，性能提高了不少。 