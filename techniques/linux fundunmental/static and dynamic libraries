static library

the static libraries can be regarded as an archive file. it is linked to our program during linking rather than runtime.

How to implement a static library

for cmake, it is just some compilation commands. 

another concept of libraries are shared library. 

The problem of static library, of course, duplicates, inflexibility. 