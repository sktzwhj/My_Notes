关于输出层，如果我们使用softmax，那么所有的输出实际上就组成了关于当前问题结果（比如分类）的一个概率分布。 

除了softmax神经元外，还有sigmoid神经元以及感知器神经元。

对于感知器来说，由于输出只在0和1两个离散的值上面变化，因此有可能一点点的输入改变就使得整个网络的表现行为发生很大变化。 

sigmoid具有一定的平滑性，因此如果输入仅仅发生了比较小的变化，对最终结果并不会造成显著影响。 